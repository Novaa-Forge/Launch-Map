import 'package:flutter/material.dart';

/// set of const values used throughout the app.

const int timeUntilNextPOI = 30;
const double zoomLevelWhenSelected = 12;
const double mapCoordOffsetForPanel = 0.05;
const int secondsForDataRefresh = 900;
const int apiLimit = 100;

const primaryColor = Color(0xff0e1023);
const secondaryColor = Color(0xff2d2f40);
